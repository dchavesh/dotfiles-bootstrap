# Non-secret manifest driving wsl/08-git-config.sh — one row per identity.
# Directory-scoped (via git includeIf), mirroring ../ssh/identities.conf.
# No global git user.name/email is ever set — see git/README.md for why.
#
# name        directory                email
personal      ~/projects/personal      84217198+dchavesh@users.noreply.github.com
kolora        ~/projects/kolora        REPLACE_ME_kolora@example.invalid
university    ~/projects/university    REPLACE_ME_university@example.invalid
blite         ~/projects/blite         REPLACE_ME_blite@example.invalid
